services:
  postgres:
    image: postgres:18

    env_file:
      - .env

    ports:
      - "5433:5432"
    
    volumes:
      - pgdata:/var/lib/postgresql
    
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d gopgtest"]
      interval: 5s
      timeout: 5s
      retries: 5
      start_period: 10s
  
  api:
    build:
      context: .
      dockerfile: Dockerfile
    
    ports:
      - "8080:8080"

    env_file:
      - .env
    
    depends_on:
      postgres:
        condition: service_healthy

volumes:
  pgdata: